let getIconType;
let a = 2 * -a * 2 + 3;
let b = -2 * -a * 2 + 3 + -Math.round(c);

if (!a && !c) {
  let d = a;
}

if (!a) {
  b = 1;
}

let c = typeof g;

getIconType = function (icon) {
  if (typeof icon !== "string") {
    return null;
  }
};

let d = a && !b[c];